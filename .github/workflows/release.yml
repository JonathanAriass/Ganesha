name: Release

# Build the macOS apps and publish a GitHub Release whenever a v* tag is pushed.
#
# Two NATIVE builds (Apple Silicon + Intel) rather than one universal dmg: the
# native modules (node-llama-cpp, better-sqlite3) make universal builds fragile,
# so each arch is built on its own native runner and the Homebrew cask picks the
# right dmg per machine (`arch arm: / intel:`).
on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-14 # Apple Silicon
            arch: arm64
          - runner: macos-15-intel # Intel (macos-13 was retired Dec 2025; this is the x86_64 image, available until Aug 2027)
            arch: x64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: 'npm' }

      - name: Install dependencies
        run: npm ci

      - name: Set the version from the tag (v1.2.3 -> 1.2.3)
        run: npm version "${GITHUB_REF_NAME#v}" --no-git-tag-version --allow-same-version

      - name: Build + package the ${{ matrix.arch }} app
        env:
          NODE_OPTIONS: --max-old-space-size=4096
          # electron-builder auto-publishes in CI when GH_TOKEN is set — we publish in
          # the separate `release` job instead, so GH_TOKEN stays unset and we build
          # with `--publish never`.
          #
          # ── Sign + notarize later (when an Apple Developer account is available) ──
          # Add these as repository Actions secrets, then uncomment:
          # CSC_LINK: ${{ secrets.CSC_LINK }}                              # base64 of the Developer ID .p12
          # CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          # APPLE_ID: ${{ secrets.APPLE_ID }}
          # APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          # APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          npm run build
          npx electron-builder --mac --${{ matrix.arch }} --publish never

      - name: Upload the app zip
        uses: actions/upload-artifact@v4
        with:
          name: mac-${{ matrix.arch }}
          path: dist/*.zip
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write # create the Release and attach the zips
    steps:
      - name: Download both zips
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create / update the GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --clobber artifacts/*.zip
          else
            gh release create "$GITHUB_REF_NAME" \
              --repo "$GITHUB_REPOSITORY" \
              --title "$GITHUB_REF_NAME" \
              --generate-notes \
              artifacts/*.zip
          fi
